name: ndalaflow

description: A new Flutter project.

publish_to: 'none' # Remove this line if the package is to be published

version: 1.0.0+1

environment:
  sdk: '^3.9.0'
  flutter: '>=3.9.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.5+1
  shared_preferences: ^2.5.3
  carousel_slider: ^4.2.1
  cached_network_image: ^3.4.1
  table_calendar: ^3.0.9
  fl_chart: ^0.60.0
  intl: ^0.18.1
  cupertino_icons: ^1.0.5

  # Local storage / offline support
  sqflite: ^2.4.2
  path_provider: ^2.1.5   # <- Downgraded to match Flutter 3.35.x

  # Connectivity
  connectivity_plus: ^4.0.2

  # Firebase
  firebase_core: ^2.32.0
  cloud_firestore: ^4.17.5
  firebase_auth: ^4.16.0
  firebase_storage: ^11.1.0
